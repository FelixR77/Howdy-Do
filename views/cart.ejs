<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
</head>
<body>
    <%- include('partials/nav') %> 
    <h1> Your Cart</h1>
    <ul>
        <% cartItems.forEach(cart => { %>
            <li>
                <h2><%= cart.product.name %></h2>
                <p>Quantity: <%= cart.quantity %></p>
                <p>Price: $<%= (cart.product.price / 100) %></p>
            </li>
        <% }); %>
    </ul>

</body>
</html>